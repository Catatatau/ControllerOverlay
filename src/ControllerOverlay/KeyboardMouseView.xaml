<UserControl x:Class="ControllerOverlay.KeyboardMouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <UserControl.Resources>
        <Style TargetType="Border" x:Key="KeyStyle">
            <Setter Property="BorderBrush" Value="#E8EEF7"/>
            <Setter Property="BorderThickness" Value="1.6"/>
            <Setter Property="Background" Value="#08000000"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="KeyText">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </UserControl.Resources>

    <Border x:Name="RootBorder" Background="Transparent" Padding="0">
        <Canvas Width="270" Height="150" HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <!-- Mouse (Right side) -->
            <Canvas Canvas.Left="200" Canvas.Top="30" Width="46" Height="80">
                <!-- Body -->
                <Border Width="46" Height="80" CornerRadius="20" BorderBrush="#E8EEF7" BorderThickness="1.6" Background="#08000000" />
                <!-- Left Click -->
                <Border x:Name="LClick" Width="20" Height="30" Canvas.Left="2" Canvas.Top="2" CornerRadius="18,2,2,2" BorderBrush="#E8EEF7" BorderThickness="1.6" Background="Transparent" />
                <!-- Right Click -->
                <Border x:Name="RClick" Width="20" Height="30" Canvas.Left="24" Canvas.Top="2" CornerRadius="2,18,2,2" BorderBrush="#E8EEF7" BorderThickness="1.6" Background="Transparent" />
                
                <!-- Side Buttons -->
                <Border x:Name="Mouse5" Width="5" Height="15" Canvas.Left="-1" Canvas.Top="32" CornerRadius="2,0,0,2" BorderBrush="#E8EEF7" BorderThickness="1.6,1.6,0,1.6" Background="Transparent" />
                <Border x:Name="Mouse4" Width="5" Height="15" Canvas.Left="-1" Canvas.Top="50" CornerRadius="2,0,0,2" BorderBrush="#E8EEF7" BorderThickness="1.6,1.6,0,1.6" Background="Transparent" />
            </Canvas>

            <!-- Keyboard Grid (Left side) -->
            <Canvas Canvas.Left="10" Canvas.Top="15" Width="180" Height="110">
                <!-- Row 1: Q W E R -->
                <Border x:Name="KeyQ" Width="24" Height="24" Canvas.Left="28" Canvas.Top="0" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="Q" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyW" Width="24" Height="24" Canvas.Left="54" Canvas.Top="0" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="W" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyE" Width="24" Height="24" Canvas.Left="80" Canvas.Top="0" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="E" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyR" Width="24" Height="24" Canvas.Left="106" Canvas.Top="0" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="R" Style="{StaticResource KeyText}"/>
                </Border>
                
                <!-- Row 2: A S D F -->
                <Border x:Name="KeyA" Width="24" Height="24" Canvas.Left="34" Canvas.Top="26" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="A" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyS" Width="24" Height="24" Canvas.Left="60" Canvas.Top="26" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="S" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyD" Width="24" Height="24" Canvas.Left="86" Canvas.Top="26" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="D" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyF" Width="24" Height="24" Canvas.Left="112" Canvas.Top="26" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="F" Style="{StaticResource KeyText}"/>
                </Border>

                <!-- Row 3: Shift, Z X C V B -->
                <Border x:Name="KeyShift" Width="38" Height="24" Canvas.Left="0" Canvas.Top="52" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="Shift" Style="{StaticResource KeyText}" FontSize="10"/>
                </Border>
                <Border x:Name="KeyZ" Width="24" Height="24" Canvas.Left="40" Canvas.Top="52" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="Z" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyX" Width="24" Height="24" Canvas.Left="66" Canvas.Top="52" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="X" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyC" Width="24" Height="24" Canvas.Left="92" Canvas.Top="52" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="C" Style="{StaticResource KeyText}"/>
                </Border>
                <Border x:Name="KeyV" Width="24" Height="24" Canvas.Left="118" Canvas.Top="52" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="V" Style="{StaticResource KeyText}"/>
                </Border>

                <!-- Row 4: Ctrl, Space -->
                <Border x:Name="KeyCtrl" Width="38" Height="24" Canvas.Left="0" Canvas.Top="78" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="Ctrl" Style="{StaticResource KeyText}" FontSize="10"/>
                </Border>
                <Border x:Name="KeySpace" Width="104" Height="24" Canvas.Left="40" Canvas.Top="78" Style="{StaticResource KeyStyle}">
                    <TextBlock Text="Space" Style="{StaticResource KeyText}" FontSize="10"/>
                </Border>
            </Canvas>
        </Canvas>
    </Border>
</UserControl>
